license="${mod_license}"

[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
authors="Az040"
description='''
Adds friendly, immortal NPCs to every major biome. Each NPC has a home, a daily job,
personality, memories of every player they meet, dialogue that changes with your reputation,
gifts, and trades that rotate every day.
'''

[[dependencies.${mod_id}]]
modId="neoforge"
type="required"
versionRange="[${neo_version},)"
ordering="NONE"
side="BOTH"

[[dependencies.${mod_id}]]
modId="minecraft"
type="required"
versionRange="${minecraft_version_range}"
ordering="NONE"
side="BOTH"
