license="${mod_license}"

[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
authors="Az040"
description='''
Eva Mod 2.0 Homestead — production-ready biome NPCs with towns, friendship hearts,
birthdays, seasons, festivals, errands, mail, and safe house teleports.

IMPORTANT: Versions 1.0–1.1.x are outdated and unsupported (locate/teleport bugs).
Use 2.0.0+ only.
'''

[[dependencies.${mod_id}]]
modId="neoforge"
type="required"
versionRange="[${neo_version},)"
ordering="NONE"
side="BOTH"

[[dependencies.${mod_id}]]
modId="minecraft"
type="required"
versionRange="${minecraft_version_range}"
ordering="NONE"
side="BOTH"
