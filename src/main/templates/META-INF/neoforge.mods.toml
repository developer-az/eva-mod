license="${mod_license}"

[[mods]]
modId="${mod_id}"
version="${mod_version}"
displayName="${mod_name}"
authors="Az040"
description='''
Eva Mod 2.1 Homestead — production biome NPCs with towns, friendship hearts,
birthdays, seasons, festivals, errands, mail, guide book, and Founder's Homestead
for worlds explored before the mod was installed.

Schema-versioned saves for future updates. Versions 1.0–1.1.x are outdated.
'''

[[dependencies.${mod_id}]]
modId="neoforge"
type="required"
versionRange="[${neo_version},)"
ordering="NONE"
side="BOTH"

[[dependencies.${mod_id}]]
modId="minecraft"
type="required"
versionRange="${minecraft_version_range}"
ordering="NONE"
side="BOTH"
